class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        s = set()
        for i in range(len(nums) - 1, -1, -1):
            if nums[i] in s:
                nums.append(nums.pop(i))
            else:
                s.add(nums[i])
        return len(s)
