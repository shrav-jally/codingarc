from collections import Counter

class Solution:
    def repeatedNTimes(self, nums):
        freq = Counter(nums)
        for num, count in freq.items():
            if count > 1:
                return num
